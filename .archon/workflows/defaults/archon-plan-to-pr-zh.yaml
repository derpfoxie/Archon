# AUTO-GENERATED MIRROR — DO NOT EDIT BY HAND.
# Source: archon-plan-to-pr.yaml
# Translation table: scripts/zh-workflow-translations.yaml
# Regenerate: bun run generate:zh-workflows
name: archon-plan-to-pr-zh
description: |
  使用场景：已有一份实现计划，希望端到端执行直至 PR 完成。
  输入：计划文件路径（$ARTIFACTS_DIR/plan.md 或 .agents/plans/*.md）。
  输出：完成全面 review、可合并的 PR。

  完整流程：
  1. 读取计划、建分支、提取范围限制
  2. 验证计划研究是否仍然成立
  3. 带类型检查地实现所有任务
  4. 跑完整校验套件
  5. 用模板创建 PR、置为 ready
  6. 完整 code review（5 个并行 agent，知晓范围限制）
  7. 汇总并修复 review 发现
  8. 输出最终摘要与决策矩阵 → GitHub 评论 + 后续建议

  不适用于：从头起草计划（请用 archon-idea-to-pr）、快修、独立 review。
nodes:
  - id: plan-setup
    command: archon-plan-setup
    context: fresh
    systemPrompt: Respond in Simplified Chinese (简体中文).
  - id: confirm-plan
    command: archon-confirm-plan
    depends_on:
      - plan-setup
    context: fresh
    systemPrompt: Respond in Simplified Chinese (简体中文).
  - id: implement-tasks
    command: archon-implement-tasks
    depends_on:
      - confirm-plan
    context: fresh
    provider: claude
    model: opus[1m]
    systemPrompt: Respond in Simplified Chinese (简体中文).
  - id: validate
    command: archon-validate
    depends_on:
      - implement-tasks
    context: fresh
    systemPrompt: Respond in Simplified Chinese (简体中文).
  - id: finalize-pr
    command: archon-finalize-pr
    depends_on:
      - validate
    context: fresh
    systemPrompt: Respond in Simplified Chinese (简体中文).
  - id: verify-pr-base
    bash: |
      set -euo pipefail
      EXPECTED="$BASE_BRANCH"
      ACTUAL=$(gh pr view --json baseRefName -q '.baseRefName')
      if [ "$ACTUAL" != "$EXPECTED" ]; then
        PR_NUMBER=$(gh pr view --json number -q '.number')
        echo "Base mismatch on PR #$PR_NUMBER: expected=$EXPECTED actual=$ACTUAL — re-targeting" >&2
        gh pr edit "$PR_NUMBER" --base "$EXPECTED"
      else
        echo "PR base verified: $EXPECTED"
      fi
    depends_on:
      - finalize-pr
  - id: review-scope
    command: archon-pr-review-scope
    depends_on:
      - verify-pr-base
    context: fresh
    systemPrompt: Respond in Simplified Chinese (简体中文).
  - id: sync
    command: archon-sync-pr-with-main
    depends_on:
      - review-scope
    context: fresh
    systemPrompt: Respond in Simplified Chinese (简体中文).
  - id: code-review
    command: archon-code-review-agent
    depends_on:
      - sync
    context: fresh
    systemPrompt: Respond in Simplified Chinese (简体中文).
  - id: error-handling
    command: archon-error-handling-agent
    depends_on:
      - sync
    context: fresh
    systemPrompt: Respond in Simplified Chinese (简体中文).
  - id: test-coverage
    command: archon-test-coverage-agent
    depends_on:
      - sync
    context: fresh
    systemPrompt: Respond in Simplified Chinese (简体中文).
  - id: comment-quality
    command: archon-comment-quality-agent
    depends_on:
      - sync
    context: fresh
    systemPrompt: Respond in Simplified Chinese (简体中文).
  - id: docs-impact
    command: archon-docs-impact-agent
    depends_on:
      - sync
    context: fresh
    systemPrompt: Respond in Simplified Chinese (简体中文).
  - id: synthesize
    command: archon-synthesize-review
    depends_on:
      - code-review
      - error-handling
      - test-coverage
      - comment-quality
      - docs-impact
    trigger_rule: one_success
    context: fresh
    systemPrompt: Respond in Simplified Chinese (简体中文).
  - id: implement-fixes
    command: archon-implement-review-fixes
    depends_on:
      - synthesize
    context: fresh
    systemPrompt: Respond in Simplified Chinese (简体中文).
  - id: workflow-summary
    command: archon-workflow-summary
    depends_on:
      - implement-fixes
    context: fresh
    systemPrompt: Respond in Simplified Chinese (简体中文).
